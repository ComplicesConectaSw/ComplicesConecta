name: Preview Environment
description: Preview deployment environment

# Environment variables
env:
  url: https://preview.complicesconecta.com
  NEXT_PUBLIC_API_URL: https://api-preview.complicesconecta.com

# Secrets requeridos
secrets:
  - name: VERCEL_TOKEN
    description: Token de acceso a Vercel
    required: true
  - name: VERCEL_ORG_ID
    description: ID de la organización en Vercel
    required: true
  - name: VERCEL_PROJECT_ID
    description: ID del proyecto en Vercel
    required: true
