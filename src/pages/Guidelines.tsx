import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const GuidelinesPage = () => {
  return (
    <div className="container mx-auto p-4">
      <Card>
        <CardHeader>
          <CardTitle>Directrices de la Comunidad</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Contenido sobre las directrices de la comunidad próximamente...</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default GuidelinesPage;
